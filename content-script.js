// This file will only have access to the browsers DOM

// chrome.runtime.onMessage.addListener((request) => {
//   if (request.type === "DISTRACTED") {
//     document.body.innerHTML = `HEY! YOU'RE DISTRACTED! GET BACK TO WORK!`;
//   }
// });
